{{ form_start(form) }}
{{ form_widget(form) }}
<button type="submit" class="btn btn-primary">{{ button_label|default('Valider') }}</button>

{{ form_end(form) }}
{% block javascripts %}

	<script>
        CKEDITOR.plugins.add('content', {

            init: function (editor) {

                 editor.addContentsCss('https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/css/bootstrap.min.css');

            }

        });
	</script>

{% endblock %}
